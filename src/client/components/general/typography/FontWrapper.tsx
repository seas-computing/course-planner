import styled from 'styled-components';

/**
 * A wrapper span component that converts the font to sans-serif
 */
const FontWrapper = styled.span`
  font-family: "Roboto", sans-serif;
`;

export default FontWrapper;
