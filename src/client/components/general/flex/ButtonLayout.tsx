import styled from 'styled-components';

/**
 * A wrapper around elements to position elements in a right aligned fashion
 */
export default styled.div`
  display: flex;
  flex-direction: row-reverse;
  justify-content: space-between;
`;
