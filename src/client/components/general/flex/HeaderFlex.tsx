import styled from 'styled-components';

/**
 * A wrapper around the elements within the header, used for adding secondary
 * layers of flex positioning.
 */
export default styled.div`
  display: flex;
  flex-direction: row;
  justify-content: flex-start;
  align-items: baseline;
  align-self: center;
`;
