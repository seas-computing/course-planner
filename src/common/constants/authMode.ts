/**
 * List of valid authentication modes to be passed into Passport */

enum AUTH_MODE {
  /** Harvard Key * */
  HKEY = 'harvardkey',
  /** Harvard Key * */
  DEV = 'development',
  /** Harvard Key * */
  TEST = 'testing',
}

export default AUTH_MODE;
